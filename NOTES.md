# 技术分析

## SM4_CBC 模式加解密性能差异分析

在 CBC 模式中，**加密必然慢于解密**是由算法本身的数据依赖关系决定的，性能不对称是正常现象。

### 核心原因：数据依赖与指令级并行

分析你的代码逻辑：

- **加密 (`encrypt`)**
  每个块的加密必须等待**前一个块的密文**作为 IV（第 1 个块用初始 IV）。
  流程：`XOR(明文, IV)` → `SM4加密` → **输出密文（成为下一个 IV）**。
  这意味着一次迭代必须完全结束（密文写出）后，下一次迭代才能读取到正确的 IV，形成**严格的串行依赖链**，
  CPU 无法重叠执行多个块的计算。

- **解密 (`decrypt`)**
  每个块解密所需的 IV 是**前一个密文块**，而这个密文块在解密开始前就已经存在于输入数据中，
  **不需要等待当前块或前一个块的解密结果**。
  流程：先保存当前密文为 `next_iv`（输入已知） → `SM4解密(当前密文)` → `XOR(解密结果, iv)` → 更新 `iv = next_iv`。
  由于 `next_iv` 来自纯输入流，CPU 可以**提前读取下一个密文块**，乱序执行下一个 `SM4解密`，
  实现指令级并行。这就是解密更快的原因。

### 验证：查看代码细节

```zig
// 加密循环中，iv 的更新依赖本次加密的输出
self.sm4.encryptBlock(&xored, ...);
iv = @as(*[16]u8, @ptrCast(output[i..].ptr)).*;  // 必须等 output 就绪
```

```zig
// 解密循环中，iv 的更新仅依赖输入数据（ciphertext），不依赖 decryptBlock 的结果
@memcpy(&next_iv, ciphertext[0..SM4_BLOCK_SIZE]); // 输入早已就绪
self.sm4.decryptBlock(...);                       // 可以与前一行并行
...
iv = next_iv;
```

加密的 IV 赋值直接从刚写入的 `output` 读取，造成写后读（RAW）依赖；解密中 IV 来源是早已稳定的 `input`，没有这种依赖。

### 总结

- **CBC 加密**是串行的，吞吐量受限于单个块的加密延迟。
- **CBC 解密**可以流水线化，CPU 的乱序执行能提前计算后续块的 `decryptBlock`，隐藏延迟。

这个差异随着数据块数增加而累积，所以你测出的 1KB 数据解密快约 15% 完全符合预期。
如果想消除不对称，可以使用 CTR 等天然支持并行的模式，或者保持 CBC 但接受这一特性。
