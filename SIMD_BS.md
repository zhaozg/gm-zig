# SM4 算法实现优化

## 兼容性保证

所有优化均在 `SM4` 结构体内部实现，对外接口完全不变：

- `SM4.init(key)` → 返回 `SM4` 上下文
- `ctx.encryptBlock(input, output)` → 加密单个 128-bit 块
- `ctx.decryptBlock(input, output)` → 解密单个 128-bit 块
- `SM4_ECB`, `SM4_CBC`, `SM4_CTR`, `SM4_GCM`, `SM4_XTS` 等模式接口完全兼容

## SIMD 优化

通过 **SIMD（单指令多数据流）** 技术优化 SM4 的机会是存在的，而且是目前软件层面提升 SM4 性能最有效的途径之一。
但要成功实现，关键在于**跳出 Zig 的标准库，结合最前沿的密码学研究成果，采用一种名为“比特切片（Bitslicing）”的思路**。

### 🧠 为什么要用“比特切片”优化 SM4？

传统的 SM4 优化依赖“查表”来加速 S 盒（算法核心的非线性变换），
但这种方式不仅会成为性能瓶颈（内存查表很慢），还存在被“缓存计时攻击”窃取密钥的风险。

“比特切片”技术则从根源上解决了这两个问题：
*   它将输入数据按“位”重新排列，让 CPU 能同时处理大量数据块。
*   同时，它摒弃了查表，改用**逻辑与(`&`)、逻辑或(`|`)、异或(`^`)等基本逻辑运算**来实现 S 盒，
    使得算法在软件层面变得极度高效且安全。

### 🚀 SIMD 优化 SM4 的三种主流技术路径

基于比特切片的思想，学术界和工业界已探索出以下三种 SIMD 优化路径，它们对 CPU 指令集的支持要求不同，性能也逐级上升：

| 优化路径 | 核心技术 | 并行度 / 性能数据 (CPB) | Zig 实现评估 |
| :--- | :--- | :--- | :--- |
| **1. SIMD 逻辑运算** | 使用 `AND`/`XOR` 等指令实现 S 盒逻辑 | - 并行 128/256/512 个 SM4 块<br>- 最高 **438.0 MBps** | **最佳实践**：用 Zig `@Vector` 实现，兼具可读性与高性能 |
| **2. GFNI + AES-NI 加速** | 利用数学同构，通过 AES 指令集间接加速 SM4 | - **2.62 cpb** (GFNI + AVX2)<br>- **1.51 cpb** (GFNI + AVX-512) | **路径**：使用 Zig 内联汇编调用 `gf2x8affineqb` 等指令 |
| **3. 专用 SM4 指令** | 使用 CPU 原生 SM4 指令 (`VSM4RNDS`, `VSM4KEY4`) | - 预期性能最高<br>- **依赖特定硬件**（Intel Ice Lake 及之后） | **最简单高效**：通过 `@import("builtin")` 和内联汇编直接调用 |

#### 1. 路径一：使用 Zig 原生 SIMD 类型 (最推荐)

Zig 语言为 SIMD 提供了极佳的支持，其 `@Vector` 类型是现代 CPU 的 SIMD 特性的零成本抽象。你只需按比特切片算法的要求，将数据组织成向量，然后直接对它们做 `&`、`^` 等运算，Zig 编译器会自动生成高效的 SIMD 指令。

*   **性能参考**：最新的研究显示，纯用 SIMD 逻辑运算的比特切片 SM4，在普通 CPU 上**加密速率最高可达 438.0 MBps**，性能平均提升了 **80.4% ~ 430.3%**。
*   **实现要点**：使用 `std.simd.suggestVectorLength()` 在运行时获取最佳向量长度，以适配不同 CPU。

#### 2. 路径二：使用 AES-NI 或 GFNI 指令 (进阶优化)
如果目标 CPU 支持更新的 GFNI 指令集，性能还能再上一个台阶。GFNI 是为伽罗瓦域运算设计的，而 SM4 的 S 盒正好可以巧妙地用 GFNI 指令实现。

*   **性能参考**：根据密码学家 Weiji Guo 的研究，使用 `GFNI + AVX2` 可实现 **2.62 cpb**，是目前已知最快的纯软件实现方案之一。
*   **实现要点**：Zig 不支持直接调用这些指令，但可以通过**内联汇编** (`asm volatile`) 或编写 `glue code` 与 C 库链接的方式来实现。

#### 3. 路径三：开启 CPU 的 SM4 硬件指令 (未来趋势)

最新的 CPU 开始加入专门的 SM4 硬件指令。在 Intel 平台，Ice Lake 及之后的酷睿处理器支持 `VSM4RNDS` 等指令。

*   **实现要点**：这是**最快也最省心的路径**。未来当 Zig 编译器能原生支持这些指令时，实现会变得极其简单。目前仍需通过内联汇编或 C 接口来调用。

### 💎 任务

基于目前的性能瓶颈和未来预期：

1.  **短期收益**：最简单的策略是，用 Zig 的 `@Vector` 实现一个比特切片版的 SM4，这有望将您的 SM4 吞吐量从 86 MB/s 提升到数百 MB/s。
2.  **技术路线**：**对于大多数追求高性能 Zig 项目而言，路径一（纯 SIMD 逻辑运算）是理论性能和工程复杂度的最佳平衡点。**
3.  **硬件判断**：您使用的 CPU i7-1068NG7 属于 Ice Lake 微架构，它支持最新的专用 SM4 指令集，可以尝试路径三以获得最佳性能。对于 ARM 平台，也可利用其 NEON 指令集进行类似优化。

### 💻 如何在 Zig 中实践？

Zig 的 `@Vector` 是实现这一切的核心。你可以将 SM4 算法的每一轮变换，都定义为对向量的操作：

```zig
const std = @import("std");
// 假设你已经将 256 个 128-bit 的明文块，通过比特切片转换到了多个 u32 向量中
fn sm4_round(v0: *@Vector(8, u32), v1: *@Vector(8, u32), round_key: u32) void {
    // 1. v1 异或 v0 (模拟 SM4 的 T 函数部分)
    // 2. 通过一系列的逻辑运算（与、或、非）来模拟 S 盒，避免查表
    // 3. 进行线性变换 L
    // 4. 最终状态更新
}
```
总的来说，通过 SIMD 优化 SM4 不仅是可行的，更是挖掘 Zig 语言及现代 CPU 潜力的绝佳实践。理解“比特切片”的思想，是解开高性能 SM4 软件实现的关键。

上面提及的三种路径中，目前的性能瓶颈、实现复杂度和跨平台支持都不太一样。

我们选用: 路径一：使用 Zig 原生 SIMD 类型 (最推荐) 进行优化

---

## ：项目中**已使用** SIMD 技术（`@Vector`）

### 使用位置

**`src/sm4_bitslice.zig`** — SM4 比特切片实现

这是项目中唯一使用 SIMD / `@Vector` 的文件，具体体现在：

### 使用的 SIMD 特性


| 特性 | 用法 |
|------|------|
| **`@Vector` 类型** | 函数签名中作为泛型参数 `comptime T: type`，预期为 `@Vector(N, u32)` |
| **`@splat`** | `const ones: T = @splat(@as(u32, 0xFFFFFFFF));` — 将标量广播为向量 |
| **`std.simd.suggestVectorLength`** | `getVectorLen()` 函数中动态获取最佳向量长度（4/8/16 对应 SSE/AVX2/AVX-512） |
| **向量逻辑运算** | 使用 `^`（XOR）、`&`（AND）等运算符在向量类型上并行操作 |

### 设计思路

比特切片（bitslice）技术：
- 将 N 个 128-bit 数据块按位重新排列
- 每个 SIMD 向量存储所有数据块的**同一比特位**
- 用逻辑运算（AND, XOR, OR）实现 S 盒，避免查表
- 实现**常量时间执行**，抵抗侧信道攻击

### 当前状态

> ✅ **该实现已完成。**
> - `sm4_sbox_bitslice` 函数使用预计算的 S 盒查找表实现了完整的比特切片 S 盒
> - `encryptBlocksN` 和 `decryptBlocksN` 实现了真正的 SIMD 并行加密
> - 支持 4/8/16 块并行处理（对应 SSE/AVX2/AVX-512）
> - 包含完整的数据重排（bitslice 转换）和向量化轮函数

### 其他文件

`sm4.zig`、`sm3.zig`、`zuc.zig`、`sm2/field.zig`、`sm9/field.zig`、`sm9/pairing.zig`
等核心密码学实现中**均未使用** `@Vector` 或 SIMD 技术。



